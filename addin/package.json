{
  "name": "ow-deck-builder-addin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "office-addin-dev-server --port 3000 --document src/taskpane.html || npx http-server -S -p 3000 .",
    "sideload": "npx office-addin-debugging start manifest.xml",
    "validate": "npx office-addin-manifest validate manifest.xml"
  },
  "devDependencies": {
    "office-addin-debugging": "^5.0.0",
    "office-addin-manifest": "^1.13.0"
  }
}
